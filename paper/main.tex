\documentclass[a4paper,onecolumn,11pt,titlepage,unpublished]{quantumarticle}
\pdfoutput=1

\usepackage[english]{babel}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{array}
\usepackage{xspace}

\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,calc,shapes.geometric,backgrounds}
\usepackage{quantikz}

\usepackage{listings}
\usepackage{xcolor}

\usepackage[numbers,sort&compress]{natbib}
\usepackage{hyperref}

\emergencystretch=3em

\definecolor{pykw}{RGB}{0,63,136}
\definecolor{pystr}{RGB}{148,54,52}
\definecolor{pycom}{RGB}{100,100,100}
\definecolor{pybkw}{RGB}{98,84,148}
\definecolor{codebg}{RGB}{248,248,245}
\definecolor{codeframe}{RGB}{176,176,176}
\definecolor{irkw}{RGB}{0,63,136}

\lstdefinestyle{pyqbase}{
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\color{pykw}\bfseries,
  keywordstyle=[2]\color{pybkw},
  stringstyle=\color{pystr},
  commentstyle=\color{pycom}\itshape,
  numberstyle=\tiny\color{pycom},
  backgroundcolor=\color{codebg},
  frame=single,
  rulecolor=\color{codeframe},
  framerule=0.4pt,
  xleftmargin=1.2em,
  xrightmargin=0.6em,
  framesep=0.6em,
  breaklines=true,
  breakatwhitespace=true,
  postbreak=\mbox{\textcolor{pycom}{$\hookrightarrow$}\space},
  showstringspaces=false,
  showtabs=false,
  tabsize=4,
  captionpos=b,
  aboveskip=1.1em,
  belowskip=1.1em,
}

\lstdefinestyle{pyqpython}{
  style=pyqbase,
  language=Python,
  morekeywords=[2]{Protocol, runtime_checkable, isinstance, assert, self, None, True, False, print, with, as, partial},
  morestring=[b]{"""},
}

\lstdefinestyle{pyqjson}{
  style=pyqbase,
  language=,
  morekeywords={Program, Module, Register, RegType, Span, Ref, QRAM, Resource, Primitive, Load, Call, Repeat, Control, Adjoint, bits, uint, sint, rational, version, entry, modules, body, registers, resources, attributes, locals, name, count, parts, kind, width, start, tag, op, args, type},
  keywordstyle=\color{pybkw},
}

\lstdefinestyle{pyqir}{
  style=pyqbase,
  language=,
  morekeywords={DEF, ENDDEF, QRAMDECL, QINIT, CREG, H, X, Y, Z, S, T, RX, RY, RZ, controlled_by, DAGGER, ENDDAGGER, REPEAT},
}

\lstset{style=pyqpython}

% compact, print-friendly table helpers
\newcommand{\tabhead}[1]{\textbf{#1}}
\newcommand{\ok}{\ensuremath{\checkmark}}
\newcommand{\noent}{\textendash{}}
\newcommand{\half}{\ensuremath{\circledcirc}}
\newcommand{\pqlang}{pyqecclang\xspace}

% cross-reference helpers
\newcommand{\secref}[1]{Section~\ref{#1}}
\newcommand{\figref}[1]{Fig.~\ref{#1}}
\newcommand{\tabref}[1]{Table~\ref{#1}}
\newcommand{\lstref}[1]{Listing~\ref{#1}}

\title{pyqecclang: An Embedded Quantum Programming Language for Scientific Computing}

% TODO: fill in real author information before submission
\author{Author One}
\affiliation{Affiliation to be determined, City, Country}
\author{Author Two}
\affiliation{Affiliation to be determined, City, Country}

\begin{document}

\begin{abstract}
Quantum algorithms for scientific computing---quantum linear-system solvers, Hamiltonian simulation, and the rapidly growing family of quantum ordinary- and partial-differential-equation (ODE/PDE) solvers---have a structure that mainstream quantum programming languages do not support well.
These algorithms are stated relative to \emph{mathematical access models} (block-encodings, sparse-access oracles, quantum random-access memories) whose physical realizations are deliberately left open; they compose deep hierarchies of subroutines whose adjoint and controlled versions must remain available; and they carry precision parameters and structural promises that must travel with the code that produced them.
We present \pqlang, a Python-embedded language that addresses these needs through three coupled mechanisms: (i) a \emph{pluggable oracle} design in which abstract oracles are declared with one of nine named paradigms, carried symbolically as open modules in a register-level intermediate representation (RIR), and linked to concrete implementations---gate networks, QRAM resources, or alternative data-loading strategies---by a separate binding pass; (ii) \emph{algorithm protocols}, structural interfaces through which algorithm libraries declare, check, and compose their input models without extending the core language or its IR; and (iii) a library of \emph{quantum scientific computing primitives}---block-encoding algebra, qubitization/QSVT with a self-verifying quantum signal processing phase engine, quantum data loading, reversible fixed-point arithmetic, and a compiler from pure Python numerical functions to reversible circuits---out of which six distinct ODE solver families and several nonlinear PDE paths are assembled.
The same open program can be partially bound, re-bound to different implementations, exported to a modular textual IR, executed on native register-level backends---including a new RIR-native interpreter that ships with PySparQ---or checked by a dependency-free reference simulator.
We survey existing quantum programming languages and frameworks, derive language requirements from the structure of quantum scientific computing itself, and compare feature and algorithm coverage in detail.
We verify the entire library numerically on real backends: a campaign of 473 experiments in thirteen groups executes every algorithm family through up to four independently implemented paths---among them an RIR-native interpreter now shipped with PySparQ and a \texttt{Circuit.to\_matrix} unitary export contributed to UnifiedQuantum, both created for this campaign---and certifies implementations against truncated-oracle references at register-exact scale, with implementation and method errors reported separately.
The campaign surfaced and recorded two genuine defects (an oblivious-amplification iteration and a Schr\"odingerization momentum sign), which we report openly alongside the certified results; parameter-scaling studies, the repair of the recorded defects, and native support for resource estimation remain future work.
\end{abstract}

\maketitle

\input{sections/introduction}
\input{sections/requirements}
\input{sections/related}
\input{sections/language}
\input{sections/oracles}
\input{sections/primitives}
\input{sections/ode}
\input{sections/casestudies}
\input{sections/implementation}
\input{sections/discussion}

\section*{Acknowledgments}
% TODO: acknowledgments.

\appendix
\input{sections/appendix}

\bibliographystyle{quantum}
\bibliography{refs}

\end{document}
